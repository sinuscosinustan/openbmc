# We will use a generic DTS for any X470D4U board. If someone wants
# sensor values that aren't on the default board, then another
# device tree is required. NC-SI should be supported on all boards with
# the generic device tree.
KMACHINE = "aspeed"
KERNEL_DEVICETREE = "aspeed/${KMACHINE}-bmc-asrock-${MACHINE}.dtb"

UBOOT_DEVICETREE = "ast2500-evb"
UBOOT_MACHINE = "evb-ast2500_defconfig"

require conf/machine/include/ast2500.inc
require conf/machine/include/obmc-bsp-common.inc
require conf/machine/include/asrock.inc
